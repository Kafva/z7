// Modulo handling negative input
// pub fn mod(k: i32, n: usize) usize {
//     if (k >= 0) {
//         return k;
//     } else {
//         const k_ = @intCast(k);
//         const d: usize = @intCast(n + k);
//         return d % n;
//     }
// }
